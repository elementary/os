name: Daily Builder

on:
  push:
    branches:
      - add-build-system

  schedule:
    - cron:  '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Clone
      uses: actions/checkout@v1

    - name: Build
      run: sudo ./workflows.sh daily
