name: Stable Builder

on:
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone
        uses: actions/checkout@v1

      - name: Build
        run: ./workflows.sh stable "$GITHUB_REF"
