name: Stable Builder
on:
  push:
    # craete an official stable build based on a pushed tag.
    - tags: '*'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: ./workflows.sh stable